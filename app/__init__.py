# app package
#
# SemCache-Router: an OpenAI compatible caching and reliability gateway that
# sits in front of LLM provider APIs.
#
# Will hold: the package version string. Nothing else. Import time side effects
# are deliberately avoided so that importing a module never opens a connection.
